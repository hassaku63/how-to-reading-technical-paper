# OS-specific (macOS)
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes
.fseventsd
.VolumeIcon.icns

# OS-specific (Windows)
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# OS-specific (Linux)
*~
.Trash-*/
.nfs*

# Go build artifacts
bin/
build/
dist/
coverage.out

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of `go build` / `go test` (e.g., pprof, coverage)
*.out

# Go workspace files (Go 1.18+)
go.work
go.work.sum

# Dependency directories (if used)
vendor/
Godeps/
